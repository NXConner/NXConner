import { Client } from 'pg-parameters';
import Changeset from '../changeset';
export interface IChangesetValidation {
    shouldExecute: boolean;
    messages: string[];
}
export default class ChangelogRepository {
    private client;
    private lockId;
    private tablesPromise;
    constructor(client: Client | any);
    aquireLock(): Promise<boolean>;
    releaseLock(): Promise<void>;
    validateChangeset(changeset: Changeset): Promise<IChangesetValidation>;
    getChangeset(file: string, name: string): Promise<Changeset>;
    executeChangeset(changeset: Changeset): Promise<void>;
    insertChangeset(changeset: any): Promise<void>;
    private ensureMigrationTables();
    private createLockTable();
    private createChangesetTable();
    private createChangelogTable();
}
