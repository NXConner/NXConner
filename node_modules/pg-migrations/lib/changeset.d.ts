export default class Changeset {
    file?: string;
    name?: string;
    executionType?: 'once' | 'always' | 'change';
    context?: string;
    script?: string;
    hash?: string;
    constructor(properties?: any);
    formatName(): string;
}
