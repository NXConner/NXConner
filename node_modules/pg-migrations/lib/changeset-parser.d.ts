import Changeset from './changeset';
export default class ChangesetParser {
    static parseFile(filename: string, basePath?: string): Changeset[];
    static parseFileContent(filename: string, content: string): Changeset[];
}
