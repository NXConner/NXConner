export interface IQuery {
    query: string;
    arguments: any[];
}
export default class QueryBuilder {
    static buildQuery(query: string, parameters?: any): IQuery;
    static buildInsert(tableName: string, fields: any, returning?: string[]): IQuery;
}
